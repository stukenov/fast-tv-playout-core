server {
  listen 8080;
  server_name _;
  root /var/www;

  location /hls/ {
    add_header Access-Control-Allow-Origin *;
    add_header Cache-Control "max-age=3";
    types { application/vnd.apple.mpegurl m3u8; video/mp4 m4s; }
    try_files $uri =404;
  }
}


